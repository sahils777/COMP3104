language: node_js
node_js:
  - "stable"

script:
  - npm install  # Install dependencies
  - npm run build  # Build your project

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: main
